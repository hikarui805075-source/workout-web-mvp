# workout-web

筋トレ記録 Web MVP（Vite + React + Dexie + Recharts）。案件メタは親フォルダの `DISPATCH.md` を参照。

## 開発

```bash
npm install
npm run dev
```

## テスト・ビルド

```bash
npm test
npm run build
```

## Vercel

`vercel.json` で SPA フォールバック済み。`npm run build` の `dist/` をそのままデプロイ。
